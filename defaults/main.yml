---

percona_mirror_source: "rsync.percona.com::rsync/"
percona_mirror_cron_disabled: true
percona_mirror_cron_minute: "{{ 59 | random(seed=inventory_hostname) }}"
percona_mirror_cron_hour: "{{ 23 | random(seed=inventory_hostname) }}"
